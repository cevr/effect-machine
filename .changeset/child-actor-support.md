---
"effect-machine": minor
---

Add child actor support: `self.spawn()` from handlers, `actor.system` on every ActorRef, implicit system creation for `Machine.spawn`, and automatic lifecycle coupling to state scope
