---
"effect-machine": minor
---

Add named guards, guard-aware `can`, Effectful transition handlers with a `never` error contract, stable eventless transitions, typed machine input and output, scoped `Machine.run` composition, cluster input adapters, actor lifecycle observation, retained latest transitions, and automatic actor system cleanup after terminal exits.
